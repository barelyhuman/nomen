{
  "name": "nomen-mono",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "play": "pnpm --filter=playground run dev",
    "test": "pnpm -r run test"
  }
}
