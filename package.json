{
  "name": "nomen-mono",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "play": "pnpm --filter=playground run dev",
    "next": "pnpm --filter=nomen-js run next",
    "test": "pnpm -r run test"
  }
}
